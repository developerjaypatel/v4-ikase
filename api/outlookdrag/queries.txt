
-----  DB -----
 Create DATABASE outLookMailDB 

---- email table ------ 

CREATE TABLE `emails` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `file_name` VARCHAR(255),
  `subject` VARCHAR(255),
  `message` TEXT,
  `is_html` TINYINT(1) NOT NULL DEFAULT 0,
  `created_at` DATETIME
);
 

composer require zbateson/mail-mime-parser

